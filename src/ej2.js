import { resolvente, areaCirculo, sumar, restar } from './modules/matematica.js';

resolvente(1, -3, 2) // Raíces reales distintas
resolvente(1, -2, 1) // Raíz doble
resolvente(1, 2, 5)  // Raíces complejas

areaCirculo(12)

sumar(16, 16)
restar(20, 8)